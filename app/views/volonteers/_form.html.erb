<%= form_for(@volonteer) do |f| %>
  <% if @volonteer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@volonteer.errors.count, "error") %> prohibited this volonteer from being saved:</h2>

      <ul>
      <% @volonteer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :surname %><br>
    <%= f.text_field :surname %>
  </div>
  <div class="field">
    <%= f.label :age %><br>
    <%= f.number_field :age %>
  </div>
  <div class="field">
    <%= f.label :city %><br>
    <%= f.text_field :city %>
  </div>
  <div class="field">
    <%= f.label :volontary %><br>
   <%= collection_select(:volonteer, :volontary_type_id, VolontaryType.all, :id, :name, {prompt: true}) %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
